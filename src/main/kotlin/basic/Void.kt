package basic

fun unitEqualVoidInJava(): Unit {
    println("Unit in Kotlin is equal to Void in Java")
    println("void in Java is primitive, so in stack")
    println("Void in Java is reference, so in heap ")
    println("Unit in Kotlin is reference and singleton object")
}